# deps
node_modules/

# next.js build output
.next/
out/

# env — never commit secrets (.env.example is the tracked template)
.env*
!.env.example

# vercel
.vercel/

# typescript
*.tsbuildinfo

# misc
.DS_Store

# branding demo mockups carry real customer PII (names, addresses, phones,
# emails, insurance claim numbers) — kept OUTSIDE the repo at
# ../excel-ces-recovery/. This is a backstop. See branding/README.md.
branding/*.html
