# deps
node_modules/

# next.js build output
.next/
out/

# env — never commit secrets (.env.example is the tracked template)
.env*
!.env.example

# vercel
.vercel/

# typescript
*.tsbuildinfo

# misc
.DS_Store

# branding demo mockups carry real customer PII — never commit. The reference
# copy lives outside the repo at ../excel-ces-recovery/. See branding/README.md.
branding/*.html
